networks:
  vulnet:
    ipam:
      config:
        - subnet: 10.0.0.0/24

services:
  #starthere-demo:
  #   build:
  #     context: ./starthere
  #   ports:
  #     - "31337:31337"
  #   networks:
  #     - vulnet
  #web01-demo:
  #  build:
  #    context: ./web01-demo
  #  networks:
  #    - vulnet
  web02:
    build:
      context: ./web02
    networks:
      - vulnet
    ports:
      - "22:22"
      - "1337:1337"
